language: java
addons:
  sonarcloud:
    organization: "marinakrivonosova"
    token:
      secure: travis encrypt 0f555038c97ed706c28f047e38d84b269a82a89b

script:
  # the following command line builds the project, runs the tests with coverage and then execute the SonarCloud analysis
  - mvn clean org.jacoco:jacoco-maven-plugin:prepare-agent install sonar:sonar -Dsonar.projectKey=marinakrivonosova_diary
